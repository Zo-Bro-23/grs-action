import { RequestHeaders } from "@octokit/types/dist-types/RequestHeaders";
import { RequestMethod } from "@octokit/types/dist-types/RequestMethod";
import { RequestParameters } from "@octokit/types/dist-types/RequestParameters";
import { Url } from "@octokit/types/dist-types/Url";
/**
 * The `.endpoint()` method is guaranteed to set all keys defined by RequestParameters
 * as well as the method property.
 */
export declare type EndpointDefaults = RequestParameters & {
    baseUrl: Url;
    method: RequestMethod;
    url?: Url;
    headers: RequestHeaders & {
        accept: string;
        "user-agent": string;
    };
    mediaType: {
        format: string;
        previews: string[];
    };
};
